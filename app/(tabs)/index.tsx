import { ScrollView, View } from "react-native";
import { Text } from "react-native-paper";

export default function TabsIndexScreen(){
    return(
        <ScrollView>
            
            <View>
                <Text>Tabs screens </Text>
            </View>
        </ScrollView>
    )
}