import { ScrollView, View, Text } from "react-native";

export default function DrawerIndex() {
    return(
        <ScrollView>
            <View>
                <Text>A Drawer here</Text>
            </View>
        </ScrollView>
    )
}